require 'spec_helper'

describe Omniauth::Rebrandly do
  it 'has a version number' do
    expect(Omniauth::Rebrandly::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
